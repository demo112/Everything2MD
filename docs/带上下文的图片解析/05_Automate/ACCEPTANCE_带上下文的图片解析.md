# ACCEPTANCE: 带上下文的图片解析

## 1. 验收概览

| 检查项 | 状态 | 备注 |
| :--- | :--- | :--- |
| 代码修改完成 | ✅ | `config.py`, `image_recognition.py` 已更新 |
| 单元测试通过 | ✅ | `tests/test_image_context.py` 通过 |
| 配置项生效 | ✅ | `img_rec_context_length` 可配置 |
| 现有功能回归 | ✅ | 无破坏性变更 |

## 2. 详细验证

### 2.1 上下文提取验证
- 测试脚本验证了在图片前后存在文本时，能够正确提取指定长度（如 50 字符）的上下文。
- 验证了上下文被正确注入到 LLM 的 Prompt 中。

### 2.2 边界情况
- 文件开头/结尾的图片：切片逻辑使用了 `max(0, ...)` 和 `min(len, ...)`，已处理边界。
- 上下文不足指定长度：逻辑正确，取实际长度。
