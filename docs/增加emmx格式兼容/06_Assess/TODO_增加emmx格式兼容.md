# TODO: 增加 emmx 格式兼容

## 待办事项
1. **收集真实样本**: 寻找真实的 MindMaster .emmx 文件进行验证，确保 JSON 结构的假设覆盖了所有常见情况。
2. **图片支持**: 目前仅提取文本。emmx 中的图片通常存储在 zip 的 `media` 目录下，并在 JSON 中引用。后续可以考虑提取图片并更新 Markdown 链接。
3. **富文本支持**: 当前简单提取纯文本。如果节点包含复杂的富文本（颜色、字体），目前会被忽略。
