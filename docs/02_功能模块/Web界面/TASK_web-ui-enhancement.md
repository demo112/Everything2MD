# Web界面增强 - 任务清单 (Task)

## 1. 后端开发 (Backend)
- [ ] **端口配置**: 修改 `main.py` 启动部分，支持 `WEB_PORT` 环境变量和命令行参数。
- [ ] **日志管理器**: 实现 `LogManager` 类，包含队列和子进程管理逻辑。
- [ ] **WebSocket 接口**: 添加 `@app.websocket("/ws/logs")` 端点。
- [ ] **异步转换**: 重构 `POST /api/convert`，不再阻塞等待，而是启动后台任务并立即返回。

## 2. 前端开发 (Frontend)
- [ ] **WebSocket 集成**: 修改 `script.js`，在点击开始后建立 WebSocket 连接接收日志。
- [ ] **状态管理**: 优化“转换中”的状态显示，支持通过 WebSocket 关闭事件重置状态。
- [ ] **盘符导航验证**: 检查文件选择器在根目录 (`/`) 下的显示，确保能列出所有盘符。

## 3. 验证 (Verification)
- [ ] **实时性测试**: 运行长任务，确认日志逐行显示。
- [ ] **并发测试**: 尝试在任务运行时再次点击开始，应被拒绝或提示。
- [ ] **端口测试**: 使用非默认端口启动服务。
