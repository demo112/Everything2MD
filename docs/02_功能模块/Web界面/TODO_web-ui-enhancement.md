# Web界面增强 - 待办事项 (Todo)

## 1. 优先级: 中
- [ ] **任务取消**: 目前仅能查看日志，无法中途取消任务（后端需实现 `process.terminate()` 接口）。
- [ ] **多用户支持**: 当前为单例任务模式，多用户同时使用会冲突（需引入任务队列和 Session 隔离）。

## 2. 优先级: 低
- [ ] **日志持久化**: 将 WebSocket 日志保存到文件供后续查看。
- [ ] **UI 美化**: 使用 xterm.js 替代简单的 `<pre>` 标签，提供更真实的终端体验（颜色高亮）。
