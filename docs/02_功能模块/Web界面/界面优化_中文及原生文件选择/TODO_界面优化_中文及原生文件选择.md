# TODO: 界面优化_中文及原生文件选择

## 遗留项
无直接遗留项。

## 建议
1.  **远程模式支持**：当前方案依赖于服务端弹出窗口，仅适用于本地运行。若未来支持远程部署，需重新设计文件上传/选择逻辑（可能需要区分 Local/Remote 模式）。
2.  **国际化 (i18n)**：目前是硬编码中文。若需要支持多语言，建议引入 i18n 库（如 `vue-i18n` 或简单的 JSON 映射）。
