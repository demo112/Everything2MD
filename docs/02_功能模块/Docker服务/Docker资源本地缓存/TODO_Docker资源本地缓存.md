# TODO_Docker资源本地缓存

## 遗留问题
- [ ] **缓存清理**: `docker_resources` 目录可能会变得很大，需要定期清理过期的包。
- [ ] **版本一致性**: 如果 `requirements.txt` 更新，必须手动重新运行下载脚本，否则构建可能失败或回退到网络下载。

## 建议改进
- [ ] 集成到 CI 流程，自动检测依赖变更并更新缓存。
- [ ] 考虑压缩 `docker_resources` 以便分发。
