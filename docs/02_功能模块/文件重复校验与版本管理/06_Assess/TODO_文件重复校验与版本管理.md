# TODO: 文件重复校验与版本管理

## 1. 待办事项
- [ ] **验证 API 兼容性**: 在真实的 RAGFlow 部署环境中测试 `delete_documents` 接口。如果不支持 DELETE with Body，需改为 URL 参数传递 ID 或循环单个删除。
- [ ] **性能监控**: 观察大文件哈希计算的耗时，必要时引入分块异步计算或进度条反馈。
- [ ] **UI 优化**: 在上传列表中直观显示“跳过”、“更新”、“新增”的状态图标，提升用户体验。

## 2. 寻求支持
- **RAGFlow 运维**: 需要确认当前部署的 RAGFlow 版本及其 API 网关配置，是否允许 DELETE 请求携带 Body。
