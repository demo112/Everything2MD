# TODO: Image Recognition (解析增强)

## 待办事项
1.  **手动验证**:
    -   请运行 `python src/gui/main.py` 启动程序。
    -   在 "解析增强" 页签中配置您的 OpenAI API Key (需支持 Vision)。
    -   **务必勾选 "启用图片识别功能"**。
    -   选择一个包含图片的 Word 文档进行转换。
    -   检查生成的 Markdown 文件，确认图片下方是否生成了 "> **图解**: ..." 内容。

## 缺失配置/依赖
-   无新增外部依赖 (复用了 `httpx`)。

## 操作指引
-   **配置**: 建议将并发数设置为 2-5 之间，过高可能导致 API 429 错误。
-   **模型**: 默认使用 `gpt-4-vision-preview`。请确保您的 API Key 有权访问该模型。
-   **注意**: 转换过程中，如果图片识别失败 (如网络问题)，会在日志中显示警告，但不会中断转换流程。
