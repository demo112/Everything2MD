# TODO: RAGFlow集成增强

## 待办事项
- [ ] **UI 细节优化**: 上传进度条目前是简单的文本状态更新，可升级为真实的进度条（需 httpx 流式上传支持）。
- [ ] **Web 端支持**: 将此功能同步到 Web 端。

## 已完成
- [x] **RAGFlow 环境联调**: 已在真实 RAGFlow 环境中验证 API 调用 (List Datasets)。
- [x] **错误处理增强**: 已增加详细的 API 错误日志（含 URL）和部分弹窗提示。
- [x] **API Key 持久化**: 已实现配置保存功能，无需每次启动重新输入。
- [x] **测试完善**: 补充了 RAG 客户端和配置管理的单元测试，全项目测试通过。

## 配置说明
- 用户首次使用需输入 RAGFlow 的 API Base URL 和 API Key，系统会自动保存。
