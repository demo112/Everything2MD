# Everything2MD 项目规则 (Project Rules)

本文档定义了本项目的核心开发流程、行为准则和技术规范。所有开发活动必须严格遵循以下规则。

## 1. 核心工作流：6A 模式

本项目采用 **6A 工作流** 进行开发管理。用户输入 `6A工作流` 即可激活标准流程。

### 阶段 0: 初始化 (Initialization)

-   **目标**: 框架化项目，引导项目规范执行。
-   **操作**:
    -   检查并创建 `docs/【需求名称】/` 目录结构。
    -   确保包含所有 6A 阶段子目录。
    -   识别任务轮次（首轮/非首轮）。

### 阶段 1: Align (对齐)

-   **目标**: 模糊需求 → 精确规范。
-   **产物**: `ALIGNMENT_[任务名].md`, `CONSENSUS_[任务名].md`。
-   **关键点**:
    -   分析现有上下文。
    -   识别歧义并生成决策问题。
    -   达成最终共识，明确需求边界和验收标准。

### 阶段 2: Architect (架构)

-   **目标**: 共识文档 → 系统架构 → 模块设计 → 接口规范。
-   **产物**: `DESIGN_[任务名].md`。
-   **关键点**:
    -   系统分层设计 (L1 业务, L2 逻辑, L3 细节)。
    -   使用 Mermaid 绘制架构图、依赖图、数据流图。
    -   确保与现有架构对齐，避免过度设计。

### 阶段 3: Atomize (原子化)

-   **目标**: 架构设计 → 拆分任务 → 明确接口 → 依赖关系。
-   **产物**: `TASK_[任务名].md`。
-   **关键点**:
    -   拆分为可独立验证的原子任务。
    -   定义输入/输出契约。
    -   明确任务依赖关系（使用 Mermaid）。

### 阶段 4: Approve (审批)

-   **目标**: 原子任务 → 人工审查 → 迭代修改 → 按文档执行。
-   **产物**: `APPROVAL_CHECKLIST.md` (可选) 或确认对话。
-   **关键点**:
    -   检查完整性、一致性、可行性、可控性、可测性。

### 阶段 5: Automate (自动化执行)

-   **目标**: 按节点执行 → 编写测试 → 实现代码 → 文档同步。
-   **产物**: `ACCEPTANCE_[任务名].md`, 代码实现。
-   **关键点**:
    -   **测试优先**：先写测试，后写实现。
    -   严格遵循代码规范。
    -   逐步实施，每完成一个任务立即验证。
    -   遇到不确定问题立即中断。

### 阶段 6: Assess (评估)

-   **目标**: 执行结果 → 质量评估 → 文档更新 → 交付确认。
-   **产物**: `FINAL_[任务名].md`, `TODO_[任务名].md`。
-   **关键点**:
    -   验证所有验收标准。
    -   评估代码、测试、文档质量。
    -   记录遗留问题 (TODO) 并提供解决指引。

------------------------------------------------------------------------

## 2. 通用开发规则

### 2.1 Docker 规范

-   **容器管理**: 项目发生变化时，必须停止并删除旧容器，重新构建运行。
-   **国内环境**:
    -   必须配置国内镜像源。
    -   必须设置 `TZ=Asia/Shanghai` 及 UTF-8 字符集。

### 2.2 对话与沟通

-   **语言**: 所有对话必须使用 **中文**。
-   **总结**: 每轮对话结束时必须总结：
    -   提出的问题。
    -   达成的共识。
    -   遗留问题/探讨点。

### 2.3 项目维护

-   **第三方工具**: 调用前必须阅读官方最新接口文档。
-   **日常维护**: 每次变更时同步进行：
    -   代码格式化。
    -   注释更新。
    -   依赖管理。
    -   临时文件清理。
    -   项目框架更新。
-   **里程碑同步**: 确保文档、缓存、引用、交付件的一致性。

### 2.4 测试规范

-   **自动化测试**: 方法变更必须补充自动化测试。
-   **工具**: 使用 `pytest`。
-   **覆盖率**: 覆盖边界、异常及正常流程。
-   **命名**: 测试文件/函数均以 `test_` 开头。
-   **独立性**: 测试必须能独立运行。
-   **Bug 分析**: 发现新 Bug 时，必须分析漏测原因并完善测试。

### 2.5 可观测性

-   **全链路日志**: 系统交付的所有功能必须具备全链路日志记录能力。
-   **标准**: 仅凭日志文件即可无损复现用户现场环境与操作路径。

### 2.6 环境规范

-   **虚拟环境**: 必须构建名为 `.venv` 的虚拟环境。
-   **命名**: 尽可能使用中文（项目名、文件夹、文件、注释等，代码标识符除外）。
-   **Python 命令**:
    -   若 `python` 不可用，使用 `py -3`。
    -   安装依赖使用 `python -m pip`。

### 2.7 文档规范

-   **Mermaid**: 节点描述和标题始终使用双引号包裹文本 (如 `Node["文本内容"]`)，换行使用 `<br/>`。
-   **同步更新**: 代码变更必须同步更新相关文档。

## 3. 安全规范

-   **API 密钥**: 必须放入 `.env` 文件，禁止提交到 Git。